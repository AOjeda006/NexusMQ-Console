packages:
  - apps/*
  - packages/*

# esbuild (dependencia de Vitest) necesita ejecutar su script de instalación para
# colocar el binario nativo de la plataforma. pnpm bloquea build scripts por
# defecto; aquí lo permitimos explícitamente (allow-list).
allowBuilds:
  esbuild: true
